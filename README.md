SELECT * FROM tasks Showed all tasks
SELECT * FROM tasks WHERE done = 1; showed no task
SELECT COUNT(*) FROM tasks; showed how many tasks are there
UPDATE tasks SET done = 1; update all rows to have 1 on done
DELETE FROM tasks WHERE done = 1 Deleted all tasks

All SQL operations that has been don on the db reflects the API